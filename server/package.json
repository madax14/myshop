{
  "name": "server",
  "version": "1.0.0",
  "description": "A BookShop in react and node.js",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "concurrently -n client,server -c yellow,red \"cd ../client && npm start\" \"nodemon server.js\""
  },
  "author": "Diego Santos",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "concurrently": "^8.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemon": "^2.0.22"
  }
}
